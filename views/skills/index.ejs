<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
    <% skills.forEach(el => { %>
      <li> <a href="skills/<%= el.id%>"><%= el.skill %></a>
      -
      <%= el.done %>
      </li>
      <% }); %>
  </ul>
  <a href="skills/new">Add New Skill</a>
  </body>
</html>
